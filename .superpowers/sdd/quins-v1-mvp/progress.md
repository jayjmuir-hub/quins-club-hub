# SDD ledger — plan: docs/plans/quins-v1-mvp.md
Task 1: complete (commits 77244cb..f912aa2, review clean)
Task 1: minor (deferred): test:integration uses POSIX inline env syntax; not Windows-cmd portable
Task 1: controller fix (post-review, asset only): crest.png downscaled 895px->400px, crest-small.png removed
Task 2: controller ruling — anon client reading 0 teams is BY DESIGN. All RLS SELECT policies (teams, clubs, events, players) require a memberships row for auth.uid(). Plan's "expect 15" assumed a member context. Implementer's adjusted expectation stands.
Task 2: derived requirement for Tasks 7/8 — app must render an explicit "signed in, no membership yet / ask an admin for an invite" state, not an empty app.
Task 2: verified auth.users trigger on_auth_user_created -> handle_new_user() auto-creates the profiles row; no app-side profile creation needed.
Task 2: complete (commits 8c8c746..dba7ad6, review clean)
Task 2: controller fix (doc only): removed publishable key literal from docs/plans/quins-v1-mvp.md
Tasks 3+4: complete (commits a0d1e20..b901b99, review clean) — combined into one commit, same file
Tasks 3+4: minor (deferred): getSession() rejection swallowed with no console diagnostic (src/lib/auth.jsx:40-43)
Tasks 3+4: minor (deferred): no test for the unmount-before-resolve race the `mounted` guard exists for
Task 5: complete (commits b901b99..32f9b5d, review clean)
Task 5: minor (deferred): noValidate makes native validation semantic-only; no whitespace-only email test; "Use a different email" keeps stale value
Task 6: fix round 1/5 (1 addressed, 0 open — surfaced failed magic-link/OAuth errors on Login; commits c9eee27..546ae93)
Task 6: complete (commits 32f9b5d..546ae93, review clean)
Task 6: minor (deferred): Login.jsx:24-28 derives state from props via useEffect; safe only because RequireAuth never resets authError
Task 6: CARRY TO TASK 8 — RequireAuth.authError is never cleared for the app lifetime; after in-SPA sign-out, Login would resurface "That sign-in link didn't work". Task 8 introduces sign-out, so it must clear it.
Task 7: fix round 1/5 (commit 7c45d99 — canEditTeam null guard, sort_order NaN guard, roleLabel precedence tests). Fix landed and 79/79 tests pass, BUT the scoped re-review was NOT run before the session paused. RESUME HERE: run review-package 7c45d99 against ce0a758..7c45d99 and dispatch re-review-prompt before marking Task 7 complete.
SESSION PAUSED 27 Jul 2026 ~11:20 UTC. Work bundled to quins-club-hub.bundle + source zip, delivered to user and written to C:\Users\Jay\GitHub\ on device "cafnet". Handoff doc written to project as claude/Quins App — Build Handoff (session state).md
Task 7: fix round 1/5 re-review — all 6 findings ADDRESSED, 35/35 scope tests green, no new breakage
Task 7: complete (commits 546ae93..7c45d99, review clean)
Task 7: minor (deferred, process): task-7-report.md was never updated with the fix-round evidence (session interruption)
Task 8: fix round 1/5 (3 addressed: mobile role label + class-token test, error text quinsRedDark, tagline desktop size; commit 55dbb81)
Task 8: fix round 2/5 (1 addressed: crest was object-fit:fill flattening the 369x400 shield in a square badge; object-contain in AppShell + Login, alt + class-token regression tests; commit 08ea8ce)
Task 8: controller visual verification — rendered real components in Chromium at 375px and 1280px via a throwaway harness (harness/, gitignored). Confirmed: role label visible at both widths, bottom tab bar <820px / top nav >=820px never both, gradient renders, crest no longer clipped, no overflow, no console errors in 10 renders. Screenshots in screenshots/ (gitignored).
Task 8: complete (commits 7c45d99..08ea8ce, review clean)
Task 8: minor (deferred): mobile role span has truncate without min-w-0; long role strings may push width rather than ellipsis
Task 8: minor (deferred): "last row hidden behind tab bar" verified by geometry (40px clearance) not visually — recheck once a real screen has enough content (Task 11+)
Task 9: fix round 1/5 — Critical Sheet stale-onClose focus-steal bug (latest-ref pattern + regression test verified RED via git stash), neutral Chip/Badge contrast #77726e->#5c5854 (6.04:1, recomputed independently), .sheet-grip ported, desktop settle keyframe restored, Empty hover #D62A3D; commit 8b834ad
Task 9: fix round 1/5 re-review — all 6 findings ADDRESSED, no new breakage
Task 9: complete (commits b1d1a27..8b834ad, review clean)
Task 9: deferred (later hardening pass): no aria-hidden/inert on background content while Sheet is open; inline icon SVGs not consolidated into a shared <Icon>
PHASE C COMPLETE. 142 tests passing. Next: Phase D (Tasks 10-13 — data-access modules, schedule, roster, dashboard).
Task 10: complete (commits 8b834ad..38eedee, review clean, no fix round)
Task 10: minor (deferred): idempotent-unsubscribe boilerplate duplicated in events.js and availability.js; extract if a third subscribe* appears
SESSION PAUSED (2nd) 27 Jul 2026. HEAD 38eedee. Tasks 1-10 complete and reviewed clean, 167 tests passing. RESUME AT TASK 11 (Schedule screen) — brief already generated at task-11-brief.md. Briefs 12 and 13 also pre-generated.
Task 11: review round 1 — spec COMPLIANT, task quality NEEDS FIXES. 0 Critical, 2 Important: (a) realtime refresh sets loading=true unconditionally so every events change repaints a spinner over the whole list, and every RSVP blanks the availability bar in EventDetail; (b) src/lib/eventFormat.js ships 9 pure exports with no direct test file, leaving the half-score rule (both halves required for a result) unverified.
Task 11: controller ruling on reviewer WARN — the design system's green "N available" count on upcoming rows is deliberately omitted. Task 10 exposes only listAvailability(eventId), so rendering it per row is an N+1. DEFERRED TO TASK 16 (availability), which owns the counts view/RPC. Not a Task 11 gap.
Task 11: minor (deferred): calendar day cell aria-label announces N events but onSelect opens only the first (Schedule.jsx:239)
Task 11: minor (deferred): past fixtures awaiting a score sort to the top of Upcoming (ascending sort, Schedule.jsx:330)
Task 11: minor (deferred): tests/app.test.jsx:93 still named "renders the schedule placeholder" though it now renders the real screen
Task 11: minor (deferred): the hidden-token responsive guard never runs with the EventDetail sheet open, so the negative-margin hero has no automated responsive assertion
Task 11: minor (deferred): AVAILABILITY_TONES duplicates the same hex per status in bar and dot (EventDetail.jsx:70-74)
Task 11: minor (deferred): Schedule.jsx is 431 lines; CalendarMonth (~120 lines) is the obvious extraction if it grows
Task 11: minor (deferred): tabs are aria-pressed toggles, not an ARIA tablist — deliberate (a half-built tablist without roving tabindex is worse), recorded as a deviation from the brief's "Tabs" wording
Task 11: OPEN PRODUCT QUESTION FOR JAY — event times render in the browser's timezone, not forced to Asia/Dubai. A committee member travelling would see shifted kick-off times. Decide before go-live.
Task 11: fix round 1/5 (2 addressed, 0 open — realtime spinner flash fixed via isFirstLoad in Schedule and a settledForEvent ref in EventDetail; tests/event-format.test.js added, 28 cases; commits a6fb3e6..448f374). Re-review: all findings ADDRESSED, no new breakage.
Task 11: controller visual verification — rendered the real Schedule/EventDetail in Chromium at 375px and 1280px via harness/. CLEAN: EventDetail hero bleed exact at both widths, no horizontal overflow, nothing CSS-hidden (scores visible on every Results row), tab bar clears the last row, crest object-contain, chip variants match the design system, quinsGreen never used as text, zero console/page errors across 20 renders. ONE IMPORTANT DEFECT FOUND — see fix round 2.
Task 11: minor (deferred): Sheet.jsx:159 uses py-4 with no env(safe-area-inset-bottom) bottom padding; the prototype has it. On a home-indicator iPhone the sheet's last line sits under the indicator. Shared component from Task 9 — fix before Task 16, which puts every add/edit form in a Sheet.
Task 11: minor (deferred, inherited): desktop month grid is 775px tall (147px cells) — faithful to the prototype's aspect-ratio:1 with no desktop override, mostly whitespace.
Task 11: fix round 2/5 (1 addressed, 0 open — calendar day cells: both variants now share a CELL_LAYOUT constant with explicit flex items-start justify-start, overriding Chromium's UA button-content centring; day number now at top 8px / left 6px in BOTH variants at 375px and 1280px, was 13-vs-8 and 66-vs-8. Button semantics, aria-label and focus ring retained. New test splits cells by tagName and asserts the alignment tokens on each group independently, so the silent-divergence case fails. commits 448f374..8baba41). Re-review: ADDRESSED, no new breakage.
Task 11: housekeeping — screenshots/ re-ignored and git rm -r --cached'd (regenerable from harness/, 2.6MB per task would bloat a public repo across 22 tasks). harness/ stays TRACKED and now carries shoot-schedule.mjs plus events/availability stubs, so the next task re-runs the browser check instead of rebuilding it.
Task 11: LESSON CARRIED TO TASKS 12/13 — a <button> used as a layout box inherits Chromium's UA content-centring and no jsdom test can see it. The fixture rows are safe (they set flex items-center explicitly). Check any new non-text interactive element in a browser.
Task 11: RECURRING TOOLING BUG — .superpowers/sdd/.gitignore was reset to "*" by tooling THREE times during this task. Each time it silently untracks the entire ledger. Restored from HEAD each time and never committed as "*", but a future session will eventually commit it unnoticed. Root cause not found.
Task 11: complete (commits ad0ca54..8baba41, review clean after 2 fix rounds + a browser visual pass)
PHASE D IN PROGRESS. 228 tests passing across 13 files. Next: Task 12 (Roster screen) — brief pre-generated at task-12-brief.md.
Task 12: review round 1 — spec ❌ (one displayed value wrong), quality NEEDS FIXES. 0 Critical (the safeguarding contact block genuinely cannot leak — verified null for both "no row" and "row with no phone/email", never routed through the error branch). 1 Important: team-pill counts are computed from the already-team-filtered set, so every unselected pill reads "· 0" the moment any pill is clicked and "All · N" undercounts.
Task 12: controller ruling on the stale-team-pill concern — the implementer was right that Schedule.jsx has the identical shape and that divergent siblings are worse than one ruling. RULING: reconcile in BOTH screens with a derived activeFilter (fall back to ALL when the selected team leaves scope). The report's "clicking All recovers it" is wrong when scope shrinks to one team, because the pill row is then hidden entirely and there is no All pill to click. Fixed in Task 12's fix round for both files.
Task 12: minor (deferred to Task 22 a11y pass): the jersey number is aria-hidden on the roster row and absent from the row's accessible name, so a screen-reader user cannot scan squad numbers in the list (Roster.jsx:104)
Task 12: minor (deferred): section-head count describes scope while a pill filters the list — three denominators on screen at once. Suggest the head follows the team filter and ignores search.
Task 12: minor (deferred): the "No players match. Try a different name, position or age group." empty state also renders when a team pill, not the search box, filtered to zero — none of the suggested actions helps then (Roster.jsx:333)
Task 12: minor (deferred): no test for the derived-selectedPlayer guarantee that a removed player closes the sheet (Roster.jsx:241)
Task 12: fix round 1/5 (3 addressed, 0 open — pill counts now derive from a search-only set and TeamPills takes a counts Map instead of a name-baked hack; Ruling A activeFilter reconciliation applied to BOTH Roster and Schedule with a test in each; Ruling B harness Playwright resolution made portable via harness/playwright.mjs; commits c82aa5f..d2916c6). Re-review: all ADDRESSED, no new breakage.
Task 12: controller visual verification (independent pass, 18 renders at 375px and 1280px). CLEAN on the Task 11 defect class — all 26 roster rows compute flex/align-items:center/text-align:left with identical geometry at both widths, so the button-as-layout-box trap did not recur. No horizontal overflow in any of 14 renders; tab bar clears the last row by 41-79px; nothing CSS-hidden (jersey numbers, "-" placeholders, positions, all captain badges visible); quinsGreen appears only as the header gradient tail, never as text; UA search-clear glyph genuinely suppressed; pill counts sensible in all four states; zero console/page errors and zero React warnings. FOUR DEFECTS — see fix round 2.
Task 12: CONTROLLER ADJUDICATION of the verifier's "Critical" contact-loading finding — DOWNGRADED TO IMPORTANT. The spinner renders while getPlayerContact is in flight, i.e. BEFORE the outcome is known, so it appears identically for a player with contact data and one without. It therefore cannot tell a parent which children have contact details on file, which is the safeguarding risk that matters. It is still a real defect: it contradicts PlayerDetail's own header comment, and Spinner is role="status" with aria-label "Loading contact details..." in an aria-live region, so a screen-reader user hears that string and then silence. Fixed in round 2 by rendering nothing until a row arrives.
Task 12: minor (deferred): the age-group grouping branch keeps listPlayers' name ordering, so the jersey column reads scrambled (2, 10, 14, -, 4, 8). Faithful to design-system 5.3, which specifies jersey sort only for the position branch — but it looks like a bug to a user. Product call for Jay.
Task 12: fix round 2/5 (5 addressed, 0 open — contact block renders nothing while in flight (Spinner import removed, test holds the promise open and asserts no role="status" and no contact/loading text); MUTED_ON_PAPER #5c5854 = 6.418:1 replaces #77726e = 4.331:1 at the three on-paper sites across Roster and Schedule, on-card instances correctly left alone; Sheet gains pb-[calc(16px+env(safe-area-inset-bottom))], verified in the BUILT css not just the class string; design-system 5.7 Call/Email row added inside the contact block behind the same guard so it is unreachable without a row, and each anchor additionally gated on its own field; harness/shoot.mjs moved to the portable loader. commits d2916c6..2a8da05). Re-review: all ADDRESSED, no new breakage, contrast ratios independently recomputed and the built CSS independently grepped.
Task 12: complete (commits f34f627..2a8da05, review clean after 2 fix rounds + an independent browser visual pass)
Task 12: NOTE — Sheet's safe-area inset is now fixed, which was flagged as a deferred minor in Task 11. Tasks 14-16 put every add/edit form in a Sheet and can rely on it.
PHASE D: 3 of 4 done (10, 11, 12). 271 tests passing across 14 files. Next: Task 13 (Dashboard) — brief pre-generated at task-13-brief.md.
Task 12 AMENDMENT (product change from Jay, 27 Jul): THE CLUB DOES NOT USE JERSEY NUMBERS. Positions and captains ARE tracked, just unpopulated — position grouping and the captain badge stay exactly as built. Verified against the live DB: players.jersey_num is nullable, players.full_name is NOT NULL, and every table is empty except clubs (1) and teams (15), so nothing had to be migrated. The COLUMN IS KEPT — nullable, costs nothing, available if the senior sides ever want squad numbers. UI-only change.
Task 12 amendment: implemented — roster row and PlayerDetail hero now show INITIALS from full_name via a new pure module src/lib/playerFormat.js (+ tests/player-format.test.js), position branch sorts by name, the Jersey number row is gone from PlayerDetail, and the dead placeholder / Infinity sort fallback / jersey search term are removed. Initials rules, each pinned by a test: middle names skipped (Faisal Al Mansoori -> FM); a hyphenated or apostrophed surname is ONE name contributing one letter (Nguyen-Fitzgerald -> N, O'Sullivan -> O); single-word name -> first two letters (Ronaldinho -> RO); empty/null -> "?"; codepoint-aware splitting. The tile is aria-hidden so a screen reader hears the name, not the initials. docs/design-system.md updated in 14 places with superseded notes INCLUDING "do not add a jersey field" on the add/edit form spec — the likeliest place Tasks 14/15 would reintroduce it. commits 12d0cfd..e1a28e6, review Approved (0 Critical, 0 Important), one fix round for a stale doc line + a missing hero assertion, re-review clean.
Task 12 amendment: reviewer independently cleared the implementer's volunteered TDD caveat — it traced the path from the mocked listPlayers result to the bucket, confirmed there is no intervening sort, and verified that deleting .sort(byName) fails the test. The caveat did not hold.
Task 12 amendment: minor (deferred): initials('...') returns '..' for a punctuation-only name; the '?' fallback covers non-string/empty/whitespace only. Aria-hidden and unreachable given full_name NOT NULL plus a real name.
Task 12: CARRY TO TASKS 14/15 — the add/edit player form must NOT offer a jersey number field. docs/design-system.md says so explicitly now.
Task 11 amendment: all event times forced to Abu Dhabi time (Jay's ruling on the open question from Task 11). CLUB_TIME_ZONE='Asia/Dubai' as an IANA zone via Intl's timeZone option, NOT a +04:00 offset; formatTime/formatLongDate/dateBoxParts routed through it; new pure helpers clubDayParts/clubToday. CalendarMonth now carries its month as {year,month} numbers instead of a Date (a Date is an instant, so getDay/getDate silently read the browser zone) — day bucketing and the today-ring are club-local. EventDetail date line gains a single quiet "· Abu Dhabi time"; deliberately NOT on list rows. Presentation only: starts_at stays timestamptz/UTC, instant comparisons untouched, no schema change. 300 tests / 15 files (was 282), build clean.
Task 11 amendment: TDD evidence is behavioural, not import-error — stashing only the 3 source files left the unfixed formatTime producing 3 different strings for one instant under UTC/New_York/Auckland (expected 3 to be 1). Tests pin instants where Dubai's day differs from UTC's (21:00Z = next Dubai day, midnight boundary, month boundary both directions, club new year), assert byte-identical output across three process zones, and include a guard-the-guard test that process.env.TZ manipulation really works — without it every zone assertion could pass vacuously. Screen-level tests derive expectations from an independent Intl formatter, not from eventFormat.js.
Task 11 amendment: browser check run with dev server AND Chromium under TZ=America/New_York (a UTC browser check would prove almost nothing). Boundary fixture 2026-07-20T21:00Z renders as JUL 21 Tue 1:00 AM and dots the 21st cell, not the 20th; today-ring landed on 28 (Dubai) not 27 (New York), demonstrating that fix live. Calendar cells uniform 40x40 / 147x147 across button and div variants — the UA content-centring trap has not reopened. Zero overflow/console/page errors across 16 shots.
Task 11 amendment: self-review corrected a wrong contrast number in a code comment — white/85% on #C21F32 is 4.632:1, not the 5.02:1 claimed. Still clears AA for normal text but the margin is thin; recomputed independently. The #5c5854 muted ruling correctly ruled inapplicable (that governs on-paper muted; this sits on the red hero gradient).
Task 11 amendment: CARRY TO TASK 14 — the event form must interpret an entered date+time as Abu Dhabi time when building starts_at. A naive new Date(`${d}T${t}`) resolves in the BROWSER's zone, so a UK coach entering 20:00 writes 19:00Z (= 23:00 Abu Dhabi) and the schedule would faithfully render that wrong instant. Reading and writing must agree; the date field should also default from clubToday(), not new Date().
Task 11 AMENDMENT (product change from Jay, 28 Jul): ALL EVENT TIMES FORCED TO ABU DHABI TIME (Asia/Dubai), regardless of viewer's browser timezone. Rationale: single-city club, one ground, "20:00" must always mean 20:00 at Zayed Sports City. Presentation-only — starts_at stays UTC timestamptz, instant comparisons untouched, no schema change.
Task 11 amendment: review round 1 — spec has TWO issues, quality NEEDS FIXES. 0 Critical. 3 Important: (1) two PRE-EXISTING tests in tests/event-format.test.js (dateBoxParts split, formatTime/formatLongDate leak check) build fixtures with the browser-local Date constructor and now fail under TZ=America/New_York — the exact failure mode this change exists to prevent, hiding only because the runner sits in UTC; (2) the club-local "today" calendar highlight has ZERO test coverage — reviewer reverted it to browser-local in an isolated copy and all 34 schedule tests still passed under TZ=America/New_York; (3) docs/design-system.md:527 still specifies UAE-is-fixed-UTC+4 offset thinking, the exact model requirement 1 rejects, and is what Task 14's event form will be built from.
Task 11 amendment: contrast INDEPENDENTLY RECOMPUTED TWICE NOW (implementer, then reviewer via full sRGB linearization) — 4.632:1 confirmed correct, clears AA for the 14px normal text actually used (threshold 4.5:1, margin 0.13). The original in-code comment claiming 5.02:1 was wrong and is already corrected.
Task 11 amendment: reviewer verified the "found already present, uncommitted, from a prior session" account is internally consistent with the diff (exact test-count arithmetic checks out, no orphaned helpers, no evidence of two different attempts) — not itself a finding, recorded for the record.
Task 11 amendment: fix round 1/5 (3 addressed, 0 open — two pre-existing tests re-anchored on Date.UTC(2026,6,23,21,0) [01:00 the 24th in Dubai, still the 23rd in UTC/NY] rather than a same-day tautology; new "rings the Abu Dhabi today, not the browser's" test pins the clock to 2026-07-20T21:00Z via vi.useFakeTimers({toFake:['Date']}), proven RED against browser-local first, GREEN against the real clubToday(); docs/design-system.md corrected at the "when" section, 4.21, and swept across 4.11/4.13/4.14 — no fixed-offset language remains except the corrected note's own back-reference to the rejected wording. commits 7c58d41..1d8525a). Re-review: all ADDRESSED, no new breakage. Re-reviewer independently re-ran the fixed and new tests under BOTH TZ=America/New_York and TZ=Pacific/Auckland (78/78 both times) rather than trusting the report, and specifically verified clubToday() reads exclusively through new Date() so the fake-timer guarantee is genuine, not a false positive.
Task 11 (Abu Dhabi timezone amendment): complete (commits 5ca6d01..1d8525a, review clean after 1 fix round)
PHASE D COMPLETE except Task 13. 301 tests passing across 15 files. Next: Task 13 (Dashboard) — brief pre-generated at task-13-brief.md.
Task 13: review round 1 — spec has 2 issues, quality NEEDS FIXES. 0 Critical. Every rulings-sensitive area verified clean: scoping ({teamIds: []} honoured), first-load-vs-refresh (stricter than Schedule's version and correctly so), Dubai-time formatting (test genuinely fails under every runner zone, not just UTC — pinned 2026-07-24T20:30:00Z, asserts 12:30 AM / Jul 25), the score-based result rule (matches Schedule's hasResult exactly), and the reported event.home column-name bug fix (verified real, cross-checked against EventDetail.jsx and events.js select('*')). 2 Important: (1) Dashboard.jsx:238-241 hardcodes "signed in as a parent" for every non-admin/non-coach role including player, contradicting roleLabel(memberships) twelve lines away on the same screen; (2) disabled placeholder Add-fixture/Add-player buttons directly contradict EventDetail.jsx:23-25's already-written ruling for the identical Task 14 dependency ("Adding a disabled or read-only affordance now would promise a control that doesn't exist yet").
Task 13: CONTROLLER RULING on the disabled-vs-omitted conflict — EventDetail's precedent governs. Omit the Add-fixture/Add-player quick actions entirely until Tasks 14/15 exist rather than showing disabled controls. The reviewer's own read agreed on the merits: these sit on the landing screen, so a dead button is the first thing a coach sees. FIX ROUND MUST APPLY THIS SAME RULE CONSISTENTLY GOING FORWARD — no disabled placeholders for not-yet-built routes anywhere in this app.
Task 13: fix round 1/5 (4 addressed, 0 open — roleLabel() replaces hardcoded "parent"; both disabled Add-fixture/Add-player buttons removed per the EventDetail precedent ruling, quick-actions card now legitimately matches design-system 5.1's two-ghost-action parent-variant shape for admin/coach too; canEdit routed through canEditTeam per visible team (teamless-coach test added); countdown interval gated on the hero being present. commits 11c6430..335451e). Re-review: all ADDRESSED, no new breakage. Re-reviewer noted the in-code comment slightly overstates WHY the teamless-coach case is refused (visibleTeams' pre-filtering empties scopedTeams before canEditTeam's own null-guard would ever fire) — end-to-end behaviour is correct, comment is cosmetically imprecise, not worth another round.
Task 13: controller visual verification (independent pass, run entirely under TZ=America/New_York to stress the timezone claim). CLEAN: Home/Away badge fix confirmed correct against the live schema (events.home is the real column); Abu Dhabi time held under the hostile zone on the hero, player view and last-result; role noun correct for player vs parent in a real render; two-ghost-action quick-actions card reads as deliberate, not sparse; no button-as-layout-box risk; no overflow; --muted correctly #5c5854 (Task 12 defect did not recur); zero console/page errors. TWO DEFECTS — see fix round 2.
Task 13: minor (deferred): FixtureRow.jsx:66 date-box weekday text (10px) is #77726e on the #f3eef5 date-box fill = 4.16:1, fails AA. Same contrast family as Task 12's defect, against the lilac box instead of paper. #5c5854 would give 6.0:1. Fix alongside the next FixtureRow touch.
Task 13: IMPORTANT FINDING — trainings and socials never leave "Fixtures to play" / the Dashboard's toPlay list, because it filters on hasResult (score present) and trainings/socials can never have a score. A week-old social sat at the top of "Upcoming" on the coach/parent dashboard and was counted in the "9 Fixtures to play" stat tile. This is a genuine semantic split hiding under one name: Schedule's "Upcoming" TAB deliberately shows unscored events regardless of date (Task 11 ruling — a match needing a score stays visible until scored) and that ruling is correct and untouched. But Dashboard's "what's coming up" list and stat tile want chronologically-future events, not merely unscored ones. RULING: fix is SCOPED TO DASHBOARD ONLY. Filter Dashboard's toPlay/stat-tile list by future date (starts_at > now), not by hasResult. Do not touch Schedule.jsx or its Upcoming tab semantics — that ruling stands as-is.
Task 13: doc drift found alongside the above — docs/design-system.md:533 still maps home -> events.is_home; the real column is events.home. Plausibly the original source of the bug this task's browser check caught. Fix in the same round.
Task 13: fix round 2/5 (1 addressed, 0 open — toPlay now filters starts_at > now instead of !hasResult, feeding both the upcoming list and the stat tile; results/lastResult untouched; docs/design-system.md corrected from events.is_home to events.home. commits 335451e..c6c196a). Re-review: ADDRESSED, no new breakage. Re-reviewer confirmed Schedule.jsx has ZERO changes in the diff (the ruling that its Upcoming-tab semantics must not move held), and confirmed the new test's fixtures are days away from the pinned "now" (max skew ±14h vs a multi-day gap) so the pass is not a UTC-runner artifact — the comparison is an epoch-instant comparison, inherently zone-agnostic.
Task 13: complete (commits 98108c7..c6c196a, review clean after 2 fix rounds + an independent browser visual pass run under a hostile timezone)
PHASE D COMPLETE (Tasks 10-13: data-access, schedule, roster, dashboard). PHASE C+D fully done. 330 tests passing across 16 files. Next: PHASE E begins — Task 14 (event create/edit/delete), brief not yet generated.
Task 14: review round 1 — spec COMPLIANT, quality APPROVED, 0 Critical, 0 Important. Every brief item and every binding ruling verified against the diff. THE HEADLINE RESULT: reviewer independently worked the Abu Dhabi write-conversion arithmetic by hand (2026-07-30 20:00 -> 2026-07-30T16:00:00.000Z) AND executed the real module under TZ=America/New_York via node -e, confirming clubWallTimeToUtc/clubDateTimeInputs is genuinely correct, not self-consistently wrong. Also independently re-ran tests/event-format.test.js (54/54) under TZ=America/New_York and tests/event-form.test.jsx (34/34) under TZ=Pacific/Kiritimati. The zero-row RLS-refusal check (maybeSingle -> !data for upsert, array.length===0 for delete) is correctly implemented on BOTH write paths with dedicated tests, and is explicitly documented as reporting-only, not access control.
Task 14: reviewer flagged (cannot verify from diff, controller to confirm separately): the live events table RLS policy — report claims "event edit" = ALL with USING and WITH CHECK can_edit_team(team_id); client code is correctly written AS IF this is true and adds no enforcement of its own. Also flagged: whether title/home columns are nullable in the live schema (the form writes null for non-match fields).
Task 14: controller visual verification (independent pass, forced TZ=America/New_York in the BROWSER, not just the process). CLEAN, zero Critical/Important. Verified real per-keystroke typing loses no characters (Sheet's stale-onClose fix holds under real interaction, not just jsdom); conditional match-only fields are absent from the DOM for training/social, not hidden; edit prefill shows Abu Dhabi time correctly under the hostile zone (stub stored 15:00Z, displayed 19:00, NOT the New-York-local 11:00); the write path proven correct at a WINTER DAY-BOUNDARY case (2026-12-15 00:30 Dubai -> 2026-12-14T20:30:00.000Z UTC) in addition to the review's summer case; delete requires genuinely two clicks with no write after click 1, and does NOT use a native confirm() (would have hung Playwright — a listener was armed and never fired); squad dropdown correctly limited to one team for a single-team coach vs all 15 for admin.
Task 14: minor (deferred): raw Supabase error messages (e.g. "new row violates row-level security policy") are shown verbatim to the user in EventForm.jsx:1623 and EventDetail.jsx:1100 with no friendly framing, unlike Schedule.jsx's "We couldn't load the schedule" wrapper. Fix alongside the next touch to either file.
Task 14: minor (deferred): invalid.teamId in EventForm.jsx:1386 is dead code — the early zero-editable-teams return guarantees the dropdown is never empty, so the aria-invalid on the select can never fire.
Task 14: minor (deferred): a malformed date rolls over rather than returning null in eventFormat.js's new code (clubWallTimeToUtc('2026-02-29', ...) silently becomes March 1) — unreachable through <input type="date"> but a real gap in the exported function's documented contract.
Task 14: minor (deferred): EventForm.jsx:1385 folds an unparseable DATE into invalid.time, so a bad date highlights the Time field instead of Date.
Task 14: minor (deferred): on a failed submit the form doesn't scroll to the first invalid field — at 375px the highlighted Opponent input can be above the fold while the alert sits at the bottom, so "the highlighted fields" points off-screen.
Task 14: minor (deferred, pre-existing Task 11 code, not introduced here): EventDetail.jsx:70,312,320 KeyValue labels and Result/Availability headings still use #77726e on white (4.75:1, passes AA but breaks the house #5c5854-on-paper convention this task's own form follows).
Task 14: minor (deferred, self-flagged by implementer): negative scores are typeable — min="0" is inert under noValidate.
Task 14: complete (commits ac67abb..df39882, review clean, 0 fix rounds, independent browser visual pass under a hostile timezone)
PHASE E STARTED. 387 tests passing across 17 files. Next: Task 15 (player create/edit/delete + contact) — brief not yet generated.
Task 15: review round 1 — spec COMPLIANT. 0 Critical. 2 Important: (1) PlayerForm's own gate is "has ANY editable squad" (editableTeams.length===0), not "can edit THIS player's squad" — the file's own header comment claims a null contact row can only mean "never entered" because the form only renders for someone who passes can_edit_team, but that check actually lives in Roster.jsx (canEditSelected), not in PlayerForm itself; not a live leak today (RLS would refuse the write server-side and Roster's gating is currently correct) but the safeguarding invariant is enforced in a different file than the one that states it; (2) the contact-field disclosure note says "Only coaches and club admins can see these" but the confirmed read policy is can_edit_team(...) OR is_own_player(player_id) — the linked player can also read their own contact row, so the sentence misstates who is walled out (not a leak, but a wrong safeguarding-adjacent disclosure shown to the person entering a minor's guardian details).
Task 15: reviewer VERIFIED THE STRONGEST CLAIM INDEPENDENTLY — the failed-contact-read edge case (getPlayerContact throwing, not returning null, while editing a player who HAS contact data on file) is genuinely handled via a three-state contactStatus that gates both rendering and the write, confirmed with file:line, and genuinely tested against a rejecting mock (not just a null one) — called this "the strongest thing in the change."
Task 15: reviewer independently re-verified the "edit access implies contact-read access" claim against the actual RLS predicates supplied (not re-queried, but reasoned through) and confirmed it holds: contact read's OR-clause is a strict superset of player edit's predicate.
Task 15: reviewer flagged (cannot verify from diff, controller to confirm before go-live): whether player_contacts.player_id has ON DELETE CASCADE from players — deletePlayer's confirm copy promises "their contact details go too" and this needs confirming against the live schema, though a wrong constraint would surface as a rejected delete (visible error), not a silent orphan.
Task 15: CONTROLLER VERIFIED LIVE — player_contacts_player_id_fkey has confdeltype 'c' (CASCADE), queried directly against pg_constraint. deletePlayer's "their contact details go too" confirm copy is accurate. That reviewer flag is closed, no code change needed.
Task 15: fix round 1/5 (4 addressed, 0 open — PlayerForm's own gate now self-enforces per-player edit access via `Boolean(player) && !canEditTeam(memberships, player.team_id)`, independent of Roster.jsx's gating, with its own refusal copy and no contact read issued when refused; disclosure copy corrected to name the player as well as coaches/admins (matches can_edit_team(...) OR is_own_player(player_id)); squad reconciliation on edit falls back to player.team_id, not editableTeams[0], closing a silent squad-reassignment risk inherited from Task 14's EventForm pattern (fixed in PlayerForm only, EventForm untouched per controller instruction); validation errors now clear on input change while write-failure errors survive typing. 7 new tests (40->47 in file, 444->451 overall), confirmed non-vacuous by stashing the fix and observing exactly those four behaviours fail. commits a982baa..db99827). Re-review: all ADDRESSED, no new breakage.
Task 15: controller visual verification (independent pass, Chromium at 375px and 1280px against HEAD db99827). CLEAN on every safeguarding check, 0 Critical, 0 Important. Verified: create form; real per-keystroke typing with zero character loss; edit-with-contact-on-file with no loading flash; edit-with-no-contact-on-file shows plain blank fields with no hint of withheld data; getPlayerContact REJECTING while editing a player WITH contact data on file confirms the contact inputs are entirely absent from the DOM and zero player_contacts writes occur; clearing a phone writes an explicit null; opening the form for a player outside the coach's editable squads refuses with distinct copy and issues no contact read; delete requires two clicks with the corrected copy; the corrected disclosure sentence is visible. TWO MINOR findings (deferred): raw Postgres error text shown verbatim in PlayerForm.jsx:447's warning copy (same family as Task 14's deferred minor, not yet fixed anywhere); Sheet focuses its Close button rather than the first field on open (Sheet.jsx:74, app-wide, pre-existing, not introduced by Task 15).
Task 15: complete (commits 7d8a098..db99827, review clean after 1 fix round, independent browser visual pass — safeguarding-focused)
PHASE E: 2 of 3 done (14, 15). 451 tests passing across 17 files. Next: Task 16 (availability RSVPs + coach team-sheet) — owns the "N available" count deferred from Task 11 and the per-event availability data Task 13's Dashboard stat tiles also deferred.
Task 16: review round 1 — spec ✅ compliant, quality Approved, 0 Critical, 0 Important. `setAvailability` upserts on (event_id,player_id); parent scoping via childPlayerIds enforced in rendering (not just RLS); coach/admin override via canEditTeam; Dubai-anchored formatting; no jersey numbers; --muted #5c5854 throughout; realtime + first-load/refresh distinction all verified against the diff and a live test/build run (472/472, build clean).
Task 16: judgment call — one component (`Availability.jsx`) serves both the parent RSVP view and the coach team-sheet, switching per-row between an editable 3-way toggle and a static label via `editable = canOverrideAll || myPlayerIds.has(player.id)`. Reviewer assessed this as low-risk: no coupling bug found, a parent viewing a team they don't belong to simply renders every row static.
Task 16: entry-point wiring — EventDetail stays presentational via a new `onOpenAvailability` prop (button shown regardless of `canEdit`, since RSVP access is broader than edit access); Schedule.jsx holds `availabilityOpen` state (same pattern as EventForm) and resets it via useEffect whenever `selectedEventId` changes, to prevent fixture B showing fixture A's stale availability sheet. Verified with a real cross-team test and independently re-verified in the browser (see below) — no race found.
Task 16: CONTROLLER VERIFIED LIVE — queried pg_policies on the `availability` table directly: `avail coach manage` (ALL, can_edit_team(event's team_id)), `avail own insert`/`avail own update` (is_own_player(player_id)), `avail read` (can_see_team(...)). Matches the client-side scoping exactly. The reviewer's "cannot verify from diff" flag on live RLS is closed.
Task 16: controller visual verification (independent pass, Chromium at 375px/1280px, plus a hostile TZ=America/New_York run for the Dubai-time check). 10 of 11 checks CLEAN with concrete measurements (button-layout centring, spinner-flash suppression, contrast #5c5854 throughout, parent/coach/foreign-coach scoping genuinely absent-not-hidden in the DOM, the availabilityOpen race resolved before any frame showed stale data, no jersey numbers, no horizontal overflow, Dubai time correct under a hostile browser timezone, zero console/page errors, the parent entry-point button present and clickable). ONE HIGH-SEVERITY DEFECT FOUND (D1) — see fix round below.
Task 16: fix round 1/5 (1 addressed, 0 open — D1: RSVP toggle clicks wrote to the DB correctly but never updated the clicked row's own UI, relying entirely on a realtime echo that never arrives promptly for the writer; `handleSet`'s `.then` now patches the saved row (which `setAvailability` already returns) into local `rows` state via a functional `setRows` updater, success-only — a refused/failed write leaves the row unchanged and surfaces the existing error alert; 2 new tests assert real `aria-pressed`/tally changes post-click and confirm no optimistic update on a rejected write, proven to fail against the pre-fix code; commit f810b35). Re-review: ADDRESSED, no new breakage, realtime/spinner-suppression logic confirmed byte-identical/untouched, functional state updater confirmed safe against stale closures.
Task 16: complete (commits 4b99eb7..f810b35, review clean after 1 fix round + an independent browser visual pass that caught and closed one High-severity defect jsdom could not see)
PHASE E COMPLETE (Tasks 14, 15, 16 — event form, player form, availability RSVPs). 474 tests passing across 19 test files. Next: PHASE F — Task 17 (Admin overview), brief not yet generated.
TOOLING NOTE: mid-session, the superpowers plugin (and its subagent-driven-development scripts: task-brief, review-package, sdd-workspace) disappeared from disk after an MCP reconnect churn — `/root/.claude/plugins/synced/superpowers` no longer existed. Continued the process manually: task briefs written directly by extracting the plan's task section, diff packages built with `git log --oneline`/`git diff --stat`/`git diff -U10` redirected to a file named `review-<base7>..<head7>.diff` in this same workspace dir, matching the scripts' own naming convention. If a future session hits this again, this manual substitution is safe — the ledger/workspace layout is unaffected either way.
Task 17: review round 1 — spec ✅ compliant, quality Approved, 0 Critical, 0 Important. `isAdmin()` gate genuinely prevents admin-only fetches from firing for a non-admin (not just hides rendered output); `listPlayers()` called with no `teamIds` arg (not `{teamIds:[]}`, which would have silently emptied it); new `listClubMembers()` follows the throw/`[]` convention and correctly omits email (profiles table has no email column, confirmed live); --muted #5c5854 throughout; no jersey numbers; no native confirm(). 491/491 tests, build clean, independently re-run by the reviewer.
Task 17: routing judgment call — Admin wired at the existing `/more` route (replacing the one-line `More()` placeholder) rather than a new `/admin` route with a 5th nav item, because `tests/nav.test.jsx` pins the nav list to exactly Home/Schedule/Roster/More. Reviewer verified this constraint is real and confirmed via `git show` that the old `/more` was a bare placeholder for everyone — so this is a lateral move, not a regression, for non-admins (who now see "not authorised" instead of a blank heading). Acceptable interim state; a later task should build out `/more`'s everyone-facing club-info content per design-system §5.4.
Task 17: CONTROLLER ADJUDICATION — reviewer flagged (⚠️, not a fix-loop item) whether omitting an "Invite" entry point entirely (vs. the brief's "entry points to invite + manage") was a valid reading. RULING: consistent with the Task 13 ruling already on record ("no disabled placeholders for not-yet-built routes anywhere in this app") — Task 18 owns the invite flow and it doesn't exist yet. No fix needed; closed without a loop.
Task 17: CONTROLLER VERIFIED LIVE — queried pg_policies on `memberships`: `memb read` is `(profile_id = auth.uid()) OR is_admin(club_id)`, confirming `listClubMembers()`'s unfiltered query is genuinely club-wide for an admin and self-only for anyone else, matching the implementer's stated assumption exactly.
Task 17: controller visual verification (independent pass, Chromium at 375px/1280px). 8 of 9 checks CLEAN (button-layout centring on the Manage links, contrast #5c5854 throughout Admin.jsx's own text — the one #77726e hit traced to Nav.jsx's pre-existing inactive labels, untouched by this task, not a new regression; isAdmin gate genuinely blocks all admin data from the DOM for coach/parent/player, not just hides it; no jersey numbers; no overflow/nav-bar clipping despite `/more` going from a one-line stub to real content height; loading/error/retry states all genuine; nav still exactly 4 items; zero console errors). ONE HIGH-SEVERITY DEFECT FOUND (D1) — see fix round below.
Task 17: fix round 1/5 (1 addressed, 0 open — D1: the "Manage roster & players"/"Manage schedule & fixtures" links were plain `<a href>` tags, never intercepted by React Router, causing a full hard page reload (confirmed via a real Chromium click wiping a `window.__navMarker`) instead of client-side navigation, destroying all app state on every click; both now use `<Link to="...">` from react-router-dom, matching Dashboard.jsx's existing correct pattern, byte-identical classNames; new MemoryRouter-based tests added with an honest comment that jsdom can't observe hard-vs-soft navigation directly — that proof came from the browser check, not the unit test; commit 3d34bdf). Re-review: ADDRESSED, no new breakage, like-for-like swap confirmed, 493/493 tests, build clean.
Task 17: complete (commits 299a994..3d34bdf + e860fb8 harness commit, review clean after 1 fix round + an independent browser visual pass that caught and closed one High-severity navigation defect jsdom could not see)
PHASE F STARTED. 493 tests passing across 20 test files. Next: Task 18 (Invite flow — create + accept), brief not yet generated. Owns a new `invites` table migration + RLS + a SECURITY DEFINER accept_invite(token) function.
Task 18: CONTROLLER-APPLIED MIGRATION — the controller applied the `invites` table + RLS + `accept_invite(token)` RPC directly against the live Supabase project BEFORE dispatching an implementer, rather than delegating schema/RLS work to a subagent, because this is the first task in the build that touches the database and a bad RLS predicate fails silently (wrong rows returned, not a thrown error). Schema: `invites(id, club_id, email, role, team_id, player_id, token uuid default gen_random_uuid(), created_by, created_at, accepted_at)`, check constraint `role='admin' OR team_id IS NOT NULL` (mirrors memberships' own NOT NULL team_id for non-admin roles). RLS: `invites manage` (ALL, is_admin(club_id)) + `invites read own` (SELECT, lower(email) = lower(auth.jwt()->>'email')) — the invitee's read scope is their own verified login email, not a client-supplied value. `accept_invite(uuid)`: SECURITY DEFINER, verifies token exists + not yet accepted + caller's authenticated email matches the invite's email (all inside the function, `for update` row lock against concurrent accepts), inserts the membership row, stamps accepted_at, returns the new membership row.
Task 18: CONTROLLER SECURITY FIX MID-MIGRATION — Supabase's default privileges auto-grant EXECUTE on every new public-schema function to both `anon` and `authenticated`, regardless of an explicit `REVOKE ALL ... FROM PUBLIC` (that only revokes the PUBLIC pseudo-role's implicit grant, not each role's own default-privilege grant). `get_advisors` surfaced this immediately after the first apply. Since accept_invite performs a real WRITE (unlike this schema's existing read-only SECURITY DEFINER helpers, which are harmless booleans), explicitly ran `REVOKE EXECUTE ... FROM anon` afterward — verified via `information_schema.role_routine_grants` that only `authenticated`/`service_role`/`postgres` can call it now. Worth remembering for any future SECURITY DEFINER function this build adds: an explicit anon revoke is a separate, necessary step, not implied by revoking from PUBLIC.
Task 18: review round 1 — spec ✅ compliant, quality Approved, 0 Critical, 0 Important. Database untouched by the implementer (confirmed via diff — no .sql/migration files); `createInvite` doesn't generate `token` client-side (reads the column default back); `team_id` is null exactly for admin-role invites, matching the live check constraint, and unreachable from the UI (age-group field genuinely hidden, not just for admin role); `acceptInvite` calls `supabase.rpc('accept_invite', { _token: token })` with the exact live parameter name; no jersey numbers; --muted #5c5854; InviteForm opens in the shared Sheet. 525/525 tests, build clean, independently re-run by the reviewer.
Task 18: architectural decision scrutinized — `src/App.jsx` restructured from one shared `<AppShell><Routes>>` to each route wrapping its own `<AppShell>` individually, so `/accept-invite/:token` could exist as a sibling OUTSIDE any AppShell (AppShell refuses to render its children at all until memberships.length > 0, which a fresh invitee doesn't have — this was a real, confirmed-live bug the restructuring fixes, not a hypothetical). Reviewer traced AppShell/Nav for stateful hooks that assume a persistent mount (none found) and confirmed MembershipProvider sits above `<Routes>` so it's unaffected by the per-route remounts — but correctly flagged that jsdom can't prove there's no visible remount flash/jank on real navigation, and routed that specific question to the browser-verification pass rather than guessing.
Task 18: controller visual verification (independent pass, Chromium, full-app router scenario — not a single screen in isolation, since this task's top risk was a full-navigation behaviour). TOP-PRIORITY CHECK CLEAN: cross-route AppShell remount produces zero blank frames across 16 sampled timestamps over 4 real nav clicks, active-link styling correct immediately (no stale frame), focus never silently dropped to body, zero extra crest network requests (React keeps the same `<img>` node since `src` never changes), zero console errors across a 24-click rapid-navigation stress test. The `/accept-invite/:token` reachability fix itself confirmed live (zero-membership user reaches the real screen, not NoMembershipState). TWO DEFECTS FOUND (D1 High, D2 Low) — see fix round below.
Task 18: fix round 1/5 (2 addressed, 0 open — D1: AcceptInvite hung forever on "Accepting your invite…" under React StrictMode/`npm run dev` only (confirmed absent in a production build) because a `mounted` ref's cleanup fired on StrictMode's throwaway first-mount unmount, permanently discarding the real in-flight promise's result once the guarded second mount declined to start a new call; fixed by dropping the `mounted` flag entirely and relying solely on `calledRef` (traced step-by-step: this does NOT cause a duplicate real accept_invite call, since the guard still blocks the second mount from starting one) — new tests render under a real `<StrictMode>` wrapper and assert the full success/reload/navigate and failure/alert sequences both complete under double-invoke, the stronger of two acceptable options per the fix brief. D2: the first screen a brand-new invitee ever sees had zero club branding (deliberately outside AppShell); added the crest image + club name as a small standalone addition, without touching the AppShell-avoidance reasoning or reintroducing the reachability bug. commit 4ae52f6). Re-review: both ADDRESSED, no new breakage, 528/528 tests, build clean, StrictMode fix traced correct (no double-call risk), route placement and AppShell-avoidance comment confirmed intact.
Task 18: complete (commits 11879bf..4ae52f6 + ab9d055 harness commit, review clean after 1 fix round + an independent browser visual pass that caught and closed a StrictMode dev-only hang and a cosmetic branding gap; controller-applied database migration verified live throughout)
PHASE F COMPLETE (Tasks 17, 18 — admin overview, invite flow). 528 tests passing across 22 test files. Next: Task 19 (First-admin bootstrap — docs only, no app code), then Phase G (PWA, RLS hardening, E2E/a11y/deploy docs).
Task 19: docs-only, no app code, no test/build surface — did not run the full subagent-driven-development loop (no code for an implementer/reviewer pair to work over); controller wrote `docs/first-admin.md` directly and self-verified against the live schema before committing.
Task 19: SELF-CAUGHT BUG BEFORE COMMIT — first draft used `ON CONFLICT DO NOTHING` to make the admin-grant INSERT idempotent, but `memberships` has no unique constraint on (profile_id, club_id, role) — only a PK on a fresh uuid, which never conflicts. That statement would have silently created a SECOND admin row for Jay if he ever ran it twice, not no-op'd as the doc claimed. Rewrote as `INSERT ... SELECT ... WHERE NOT EXISTS (...)`, which is genuinely idempotent without needing a schema change. Caught by re-reading the doc against `pg_constraint` output before committing, not by a review loop — worth remembering for any future "run this SQL twice safely" doc.
Task 19: verified live before writing the doc — `auth.users` has zero rows (Jay hasn't signed in yet, so the doc's prerequisite framing is accurate, not hypothetical); `clubs` has exactly one seeded row, id `00000000-0000-0000-0000-0000000000ad`, name "Abu Dhabi Harlequins", matching the doc's hardcoded club id exactly; `memberships` columns/nullability confirmed (`team_id`/`player_id` nullable, `role` NOT NULL with the same admin/coach/parent/player check as `invites`). UI copy referenced in the doc's verification steps ("Admin overview", "Invite a member", "Not authorised", roleLabel's "No access yet") grepped directly against `Admin.jsx`/`scope.js` to avoid the doc drifting from the real strings.
Task 19: complete (commit a0232d4, docs only — no code review loop, no browser-verification pass; 528/528 tests and a clean build reconfirmed unaffected)
PHASE F FULLY COMPLETE (Tasks 17, 18, 19). 528 tests passing across 22 test files, docs/first-admin.md added. Next: PHASE G — Task 20 (PWA: installable + offline read), brief not yet generated.
